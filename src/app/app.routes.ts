import { Routes } from '@angular/router';
import { SheetTrainerComponent } from './sheet-trainer.component';

export const routes: Routes = [
  {
    path: '',
    component: SheetTrainerComponent,
  },
];
